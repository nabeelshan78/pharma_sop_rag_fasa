version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: fasa_sop_qdrant
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_data:/qdrant/storage
    environment:
      - QDRANT__SERVICE__ENABLE_TLS=false
    restart: unless-stopped
